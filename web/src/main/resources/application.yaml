spring:
  application:
    name: admission

  datasource:
    url: jdbc:mysql://localhost:3306/admission_db?useUnicode=true&characterEncoding=UTF-8&rewriteBatchedStatements=true
    username: admin
    password: admin123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        dialect: org.hibernate.dialect.MySQLDialect

server:
  port: 8081